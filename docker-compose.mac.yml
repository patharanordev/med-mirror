services:
  # ---------------------------------------------------------
  # 1. Frontend: The Web Interface (Dockerized Next.js)
  # ---------------------------------------------------------
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: med_mirror_frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    networks:
      - edge_net
    environment:
      - AGENT_SERVICE_URL=http://med_mirror_agent:8001

  # ---------------------------------------------------------
  # 2. Skin Segmentation Service (CPU / M1 Optimized)
  # ---------------------------------------------------------
  segmentation:
    build: 
      context: ./segmentation
      dockerfile: Dockerfile.mac
    container_name: med_mirror_segmentation
    restart: unless-stopped
    ports:
      - "8000:8000"
    networks:
      - edge_net
    volumes:
      - ./models:/app/models
    environment:
      - PORT=8000
      - MODEL_PATH=/app/models/segmentation

  # ---------------------------------------------------------
  # 3. Medical Agent Service (LangGraph - macOS Optimized)
  # ---------------------------------------------------------
  agent:
    build:
      context: ./agent
      dockerfile: Dockerfile.mac
    container_name: med_mirror_agent
    restart: unless-stopped
    ports:
      - "8001:8001"
    networks:
      - edge_net
    volumes:
      - ./hf_cache:/root/.cache/huggingface
      - ./agent:/app
    env_file:
      - ./agent/.env.macos
    depends_on:
      - ollama

  # ---------------------------------------------------------
  # 4. Local LLM Backend (Ollama)
  # ---------------------------------------------------------
  ollama:
    image: ollama/ollama:latest
    container_name: med_mirror_ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    networks:
      - edge_net
    volumes:
      - ~/.ollama:/root/.ollama

networks:
  edge_net:
    driver: bridge
