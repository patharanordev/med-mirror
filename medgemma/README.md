# MedMirror Directory Structure

This folder contains the **MedGemma** Service. 

## Status: Placeholder
The `docker-compose.yml` has a commented-out section for this service.
To implement:

1. Create a `Dockerfile` here.
2. Create a `main.py` (FastAPI).
3. Ensure you have downloaded the weights to `../models/medgemma` using `scripts/download_models.py`.
